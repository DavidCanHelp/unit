[package]
name = "unit"
version = "0.9.0"
edition = "2021"
description = "A software nanobot — minimal Forth interpreter that is also a self-replicating networked agent"
license = "CC0-1.0"

[dependencies]

[profile.release]
opt-level = "z"
lto = true
strip = true
